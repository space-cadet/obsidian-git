{
  "name": "obsidian-git-sync",
  "version": "1.0.0",
  "description": "Git synchronization plugin for Obsidian that works on desktop and mobile platforms",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "archive": "node scripts/build-archive.mjs",
    "test": "pnpm run build && node --test tests/*.test.mjs && node test-isomorphic-git.mjs",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "git",
    "sync",
    "mobile"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@typescript-eslint/eslint-plugin": "^5.2.0",
    "@typescript-eslint/parser": "^5.2.0",
    "builtin-modules": "^3.2.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "tslib": "2.3.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "isomorphic-git": "^1.21.0"
  }
}
