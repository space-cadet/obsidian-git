# T35f: Test, CI, and Documentation Alignment

*Created: 2026-08-11 02:03 IST*
*Last Updated: 2026-09-04 02:19:21 IST*

**Status**: 🔄 **IN PROGRESS**
**Priority**: MEDIUM
**Parent**: T35

## Scope

Make the repository's automated checks, CI workflow, README, generated
artifacts, and Memory Bank claims match the actual plugin behavior.

## Acceptance Criteria

- [ ] GitHub Actions runs the full test suite, not only build and archive.
- [ ] Add adapter, settings, concurrency, lifecycle, initialization, updater,
  and HTTP error-path coverage.
- [ ] Add a repeatable artifact-identity check for generated `main.js`.
- [ ] Correct README installation paths, command names, and unsupported or
  stale feature claims.
- [ ] Clearly separate historical mobile success from current mobile release
  acceptance and repository-size limitations.
- [ ] Remove or document legacy `old/obsidian-git` material without touching
  unrelated user data.

## Related

- T29: obsidian-git Plugin — Git Sync for Obsidian Vault
- T35: Plugin Reliability, Security, and Architecture Hardening
- `implementation-details/ci-cd-architecture.md`

## Follow-up Coverage Requirements — 2026-09-02

Add regression and integration coverage for the current T29/T35 follow-ups:

- Parse commit subjects and optional metadata from current and older release
  bodies without hiding builds when optional fields are absent.
- Verify generated development-release titles do not expose full commit SHAs.
- Verify the Changes view derives its displayed status from one repository
  snapshot and that Local/Remote switching does not run a full sidebar refresh.
- Verify remote commits load with no local `.git` directory and with a damaged
  local Git state.
- Verify repository-rebuild dry runs report local-only, remote-only, and
  conflicting files without replacing the existing `.git` or vault files.

## Session Coverage — 2026-09-03

- The production build and full test command passed after the session changes;
  the current suite reports 43 Node tests and 10 isomorphic-git checks.
- Existing coverage includes updater timeout/stale-folder tests and the
  adapter UTF-8 read regression. Keyboard layout and repository-health,
  concurrency, and rebuild dry-run coverage remain open.

## Session Coverage — 2026-09-03 Maintenance and Mobile Follow-up

- The current suite also covers the scoped logger sink, desktop native Git
  metadata access, index-repair behavior, and protected maintenance actions.
- Recorded verification remains 51 Node tests, 10 isomorphic-git checks,
  production build, artifact checks, and `git diff --check`.
- Real mobile acceptance is still open, including the existing-repository
  `refs/heads/main` failure and safe recovery behavior.

## Session Continuation — 2026-09-03

- Add regression coverage proving ignored untracked files are absent from
  status, individual staging, bulk staging, and automatic sync.
- Keep separate coverage for tracked files that later become ignored; Git must
  continue to report those files until they are explicitly removed from the
  index.
- Add operation-count and timing coverage for bounded staging batches,
  unstaging, reset, and removal.
- Add official isomorphic-git 1.41.9 compatibility coverage before adopting or
  publishing any T36 fork.

## Pocock Modularity Follow-up — 2026-09-04

- Add a conformance layer around the operation coordinator before extracting
  further modules.
- Cover every mutation entry point, concurrent-call rejection or queuing,
  cancellation on unload, stale view responses, one-time progress cleanup,
  and repository-state outcomes.
- Keep automated source checks, Obsidian desktop acceptance, Android behavior,
  and release installation as separate evidence layers.
- Use the saved report at
  `memory-bank/implementation-details/pocock-architecture-review.html`
  as the architecture-review reference; this task remains planned until the
  coverage is implemented and verified.

## Operation Lifecycle Coverage — 2026-09-04

- Added focused coordinator coverage for overlap rejection, cancellation,
  unload disposal, late-success rejection, lifecycle finalization, and
  listener isolation.
- The production build now includes the coordinator implementation, and the
  generated bundle embeds source commit `b179d02f27df13116bae3357b7001d0fa77ea57a`.
- `CI=true pnpm test` passed with 59 Node tests, artifact checks, production
  build, and 10 isomorphic-git checks.
- Remaining: source-level entry-point conformance assertions, stale-view and
  repository-state cases, and real desktop/mobile/release acceptance.

## Operation Entry-Point Conformance — 2026-09-04

- Added a TypeScript-AST-backed source conformance test covering `main.ts` and
  `GitSidebarView.ts`; repository mutation calls must be inside the shared
  `runGitMutation` callback.
- Added lifecycle assertions that plugin unload disposes the coordinator and
  that the GitManager operation signal is installed and cleared in `finally`.
- Focused conformance coverage passes. Stale-view, repository-state, and
  real desktop/mobile/release acceptance remain open.

## UI and Log Regression Coverage — 2026-09-04

- Added file-logger coverage for reconstructing structured payloads from
  persisted lines and logger coverage for live/persisted event deduplication.
- Added a sidebar regression check proving staging checkboxes never receive
  the spinner animation.
- Full verification passes with 65 Node tests, artifact identity, production
  build, 10 isomorphic-git checks, and `git diff --check`.

## Read-Model Extraction Coverage — 2026-09-04

- Added `SidebarReadModel` unit coverage for repository/branch cache keys,
  commit-detail retention, log retention, and explicit invalidation.
- Integrated the model into `GitSidebarView` without moving DOM rendering or
  Git operation calls into the new module.
- Full verification now passes with 67 Node tests, artifact identity,
  production build, 10 isomorphic-git checks, and `git diff --check`.

## Stale-Read Conformance — 2026-09-04

- Added source-level regression coverage for generation checks around async
  Log-tab and commit-detail reads.
- Full verification now passes with 68 Node tests, artifact identity,
  production build, 10 isomorphic-git checks, and `git diff --check`.

## Sidebar History, Staging, and Commit Controls — 2026-09-04

- Added source conformance coverage for rendering all retained activity-log
  entries, keeping the Local/Remote selector sticky, and avoiding a
  repository-wide status scan in the direct staging path.
- Full `CI=true pnpm test` verification passes with 72 Node tests, artifact
  identity, production build, 10 isomorphic-git checks, and `git diff --check`.
- The suite does not replace real Obsidian desktop/mobile timing, persisted
  cross-session log, or visual scroll acceptance.

## Session Closeout Note — 2026-09-04

- Automated coverage improved during this session, but many UI issues remain
  and no broad visual acceptance claim is made.
