---
source_branch: rewrite/git-backend-kiss
source_commit: f8df6f1970183df04c9b081aa2886d7776380a40
---

# T39: KISS Git Backend Rewrite

*Created: 2026-09-04*
*Status: ACTIVE*
*Priority: HIGH*
*Parent: None*

## Scope

Replace the current coupled GitManager/UI operation path with a small,
platform-neutral Git backend. The backend must be tested independently before
it is connected to the retained Obsidian UI.

## Requirements

- Examine and record the current architecture and latency sources.
- Keep the backend independent of Obsidian UI, DOM, Notices, CSS, and view
  lifecycle code.
- Provide direct operations for repository state, stage, unstage, commit,
  pull, push, sync, clone, initialize, and history.
- Keep PAT authentication available.
- Add GitHub device authorization as an alternative credential source.
- Test the backend with mocked transport and credentials first.
- Test GitHub API and Git smart-HTTP access with a real GitHub-authenticated
  credential before UI integration.
- Connect the backend to the existing UI only after the backend gates pass.
- Preserve the current plugin as the rollback/reference implementation.

## KISS constraints

- No UI rendering or Obsidian imports in the backend.
- No event bus, general-purpose cache, or multi-layer operation framework in
  the new backend unless a demonstrated requirement makes one necessary.
- One direct operation returns one complete result.
- Local status must not wait for remote history comparison unless requested.
- Single-file stage must not perform a whole-repository status scan first.

## Current progress

- [x] Created `rewrite/git-backend-kiss` from the current main baseline.
- [x] Examined the current mixed `GitManager`/`main.ts`/sidebar architecture.
- [x] Added the platform-neutral backend boundary under `src/backend/`.
- [x] Added mocked local Git, transport, device-flow, and GitHub API tests.
- [x] Added an explicit live GitHub authentication test command.
- [x] Completed the backend operation surface, including repository recovery and `.gitignore` data operations.
- [x] Ran the live GitHub OAuth-authenticated API and Git smart-HTTP clone gate through the external `gh` session.
- [x] Connected the backend to Settings and sidebar UI through a host adapter while retaining the existing presentation.
- [ ] Run the interactive GitHub device flow with a registered OAuth App client ID.
- [ ] Run real Obsidian desktop/mobile acceptance and compare latency.

## Evidence

- `node --test tests/rewrite/backend.test.mjs`: 15 tests passed, including
  targeted staging without worktree enumeration, index repair, transport,
  GitHub API, and mocked device authorization.
- `CI=true pnpm run test`: build, artifact identity, existing tests, rewrite
  tests, and the isomorphic-git smoke test passed.
- `scripts/test-rewrite-github-auth.mjs`: live GitHub API authentication and
  Git smart-HTTP clone passed using the already-authenticated external `gh`
  OAuth credential. No token is printed or stored by the test script.

## Related

- `memory-bank/product-prd.md`
- `memory-bank/implementation-details/rewrite-architecture.md`
- `src/backend/`
- `tests/rewrite/`
